// This file is not currently used - auth routes are handled in server/index.js
// Keeping for reference but commented out to avoid TypeScript errors

// import express from "express";
// import { register, verifyOtp, login } from "../controllers/authController.js";

// const router = express.Router();

// router.post("/register", register);
// router.post("/verify-otp", verifyOtp);
// router.post("/login", login);

// export default router;
